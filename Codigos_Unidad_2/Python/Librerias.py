#En este Programa vamos a importar una libreria y utilizarla

import math

print(math.sqrt(25))

#**C:** Usa `#include <math.h>`, `scanf` para entrada, `sqrt()` y `printf` para salida, requiere `main()`.  
#**Python:** Usa `import math`, `sqrt()` directamente, `print()` para salida, más simple y directo.