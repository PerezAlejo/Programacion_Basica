#Programa para introducir datos

nombre = input("¿Cuál es tu nombre? ")
print("Hola,", nombre)

#**C:** Usa `scanf` para entrada numérica, `printf` para salida, requiere `main()`.  
#**Python:** Usa `input` para entrada de texto, `print` para salida, más simple y directo.